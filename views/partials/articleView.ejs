<style>
    body {
        font-family: 'Georgia', serif;
        background: #f0f4f3;
        color: #333;
        margin: 0;
        padding: 0;
        line-height: 1.6;
    }

    .single-article-container {
        max-width: 1200px;
        margin: 15px auto;
        /* padding: 0 20px; */
    }

    .article-header {
        text-align: center;
        padding: 40px 20px;
        background: #ffffff;
        border-bottom: 5px solid #2d6a4f;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        position: relative;
        overflow: hidden;
        border-radius: 6px;
    }

    .journal-title {
        /* font-size: 2.5em; */
        font-size: calc(1.25625rem + .075vw);
        color: #2d6a4f;
        margin: 0;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        font-weight: 700;
    }
    
    .article-not-found-header {
        font-size: 1.2em;
        color: #ff6f61 !important;
        /* margin-top: 20px; */
        text-align: center;
        
    }
    .header-decor {
        position: absolute;
        bottom: -10px;
        left: 0;
        width: 100%;
        height: 10px;
        opacity: 0.7;
    }

    .article-wrapper {
        display: flex;
        gap: 30px;
        margin-top: 20px;
    }

    .article-main {
        flex: 2;
    }

    .article-card {
        background: #ffffff;
        border-radius: 15px;
        padding: 25px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.05);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .article-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(45, 106, 79, 0.2);
    }

    .article-header-section {
        border-bottom: 2px solid #3a8b66;
        /* padding-bottom: 15px; */
        margin-bottom: 20px;
    }

    .article-title {
        color: #2d6a4f;
        font-size: 1.2em;
        margin: 0 0 10px 0;
    }

    .publish-date {
        font-size: 0.9em;
        color: #666;
        font-style: italic;
    }

    .section-heading {
        color: #2d6a4f;
        font-size: 1.4em;
        margin-top: 20px;
        position: relative;
    }

    .section-heading::after {
        content: '';
        position: absolute;
        bottom: -5px;
        left: 0;
        width: 50px;
        height: 3px;
        background: #3a8b66;
        transition: width 0.3s ease;
    }

    .article-card:hover .section-heading::after {
        width: 100px;
    }

    .abstract-text,
    .keywords-text {
        font-size: 1em;
        color: #444;
        line-height: 1.7;
        margin: 10px 0;
        text-align: justify;
    }

    .article-actions {
        margin-top: 20px;
        display: flex;
        gap: 15px;
        flex-wrap: wrap;
    }

    .action-button {
        display: inline-flex;
        align-items: center;
        padding: 12px 20px;
        background: #2d6a4f;
        color: white;
        text-decoration: none;
        border-radius: 25px;
        font-size: 0.95em;
        transition: all 0.3s ease;
    }

    .action-button:hover {
        background: #3a8b66;
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(45, 106, 79, 0.3);
    }

    .action-button i {
        margin-left: 8px;
    }

    .article-sidebar {
        flex: 1;
        min-width: 250px;
    }

    .sidebar-card {
        background: #ffffff;
        border-radius: 15px;
        padding: 20px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.05);
        transition: transform 0.3s ease;
    }

    .sidebar-card:hover {
        transform: translateY(-5px);
    }

    .sidebar-title {
        color: #2d6a4f;
        font-size: 1.2em;
        margin-bottom: 15px;
        border-bottom: 2px solid #3a8b66;
        padding-bottom: 5px;
    }

    .details-list {
        list-style: none;
        padding: 0;
        margin-bottom: 20px;
    }

    .details-list li {
        font-size: 0.9em;
        color: #666;
        margin-bottom: 10px;
    }

    .details-list li strong {
        color: #2d6a4f;
    }

    .stats-box {
        margin-bottom: 20px;
    }

    .stats-box p {
        font-size: 0.9em;
        color: #666;
        /* display: flex; */
        align-items: center;
        margin: 8px 0;
    }

    .stats-box i {
        color: #2d6a4f;
        margin-right: 8px;
    }

    .stats-box span {
        font-weight: bold;
        color: #333;
    }

    .submit-button {
        display: block;
        width: 100%;
        padding: 12px;
        background: #2d6a4f;
        color: white;
        text-align: center;
        text-decoration: none;
        border-radius: 25px;
        transition: all 0.3s ease;
        font-weight: 600;
    }

    .submit-button:hover {
        background: white;
        color: #3a8b66;
        border: 1px solid #2d6a4f;
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(45, 106, 79, 0.3);
    }

    .doi-link {
        color: #2d6a4f;
        font-weight: 600;
        text-decoration: none;
    }

    .chip {
        text-align: center;
        background: #93e9c3;
        border-radius: 10px;
        padding: 2px;
        color: black !important;
        /* font-weight: 600; */
    }

    .nagivate-btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background-color: #fff;
    color: #f86e64;
    border-radius: 30px;
    text-decoration: none;
    font-weight: bold;
    transition: background 0.3s ease, color 0.3s ease;
  }

  .nagivate-btn:hover {
    background-color: #f86e64;
    color: #fff;
  }

    /* Animations */
    @keyframes fadeIn {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }

    /* Mobile Responsiveness */
    @media (max-width: 768px) {
        .article-wrapper {
            flex-direction: column;
        }

        .article-sidebar {
            min-width: 100%;
            margin-top: 20px;
        }

        .article-header {
            padding: 20px 10px;
        }

        .journal-title {
            font-size: 1.5em;
        }

        .article-card {
            padding: 15px;
        }

        .action-button {
            width: 100%;
            text-align: center;
            margin-bottom: 10px;
        }
    }
</style>

<div class="single-article-container">
    <% if (article) { %>

    <header class="article-header">
        <h1 class="journal-title ">
            <%= article.paper_title %>
        </h1>
        <div class="header-decor" style="background: linear-gradient(90deg, #2d6a4f, #3a8b66);"></div>
    </header>

    <div class="article-wrapper">
        <div class="article-card">
            <div class="article-header-section">
                <h2 class="article-title">Authors: <%= article.authors_all %>
                </h2>
                <p class="publish-date">Published on: <%= new Date(article.published_on).toLocaleDateString() %>
                </p>
            </div>
            <div class="article-content-section">
                <h3 class="section-heading">Abstract</h3>
                <p class="abstract-text">
                    <%= article.abstract %>
                </p>

                <% if (article.keywords) { %>
                    <h3 class="section-heading">Keywords</h3>
                    <p class="keywords-text">
                        <%= article.keywords %>
                    </p>
                    <% } %>

                        <% if (article.doi) { %>
                            <h3 class="section-heading">DOI</h3>
                            <p class="keywords-text">
                                <a href="<%= article.doi %>" target="_blank" class="doi-link">View DOI - (<%=
                                        article.doi %>)<i class="fas fa-link"></i></a>
                            </p>
                            <% } %>
            </div>
            <div class="article-actions">
                <a href="<%= article.doc_url %>" target="_blank" class="action-button">View PDF <i
                        class="fas fa-file-pdf"></i></a>
                <a href="<%= article.url %>" target="_blank" class="action-button">View Abstract <i
                        class="fas fa-book"></i></a>
                <% if (article.doi) { %>
                    <a href="<%= article.doi %>" target="_blank" class="action-button">View DOI <i
                            class="fas fa-link"></i></a>
                    <% } %>
            </div>
        </div>

        <aside class="article-sidebar">
            <div class="sidebar-card">
                <h4 class="sidebar-title">Issue Details</h4>
                <ul class="details-list">
                    <li><strong>Journal Name:</strong>
                        <%= article.journal_name %>
                    </li>
                    <li><strong>Volume:</strong>
                        <%= article.volume %>
                    </li>
                    <li><strong>Issue:</strong>
                        <%= article.issue %>
                    </li>
                    <li><strong>Month:</strong>
                        <%= article.month %>
                            <%= article.year %>
                    </li>
                    <li><strong>DOI:</strong>
                        <%= article.doi || 'Not Available' %>
                    </li>
                </ul>
                <div class="stats-box">
                    <p class="chip"><i class="fas fa-download"></i> <span>
                            <%= article.download_count %>
                        </span> Downloads</p>
                    <p class="chip"><i class="fas fa-eye"></i> <span>
                            <%= article.views_count %>
                        </span> Views</p>
                </div>
                <!-- <a href="#" class="submit-button">Submit Your Article</a> -->
                <a href="<%= article.doc_url %>" target="_blank" class="submit-button">View PDF <i
                        class="fas fa-file-pdf"></i></a>

            </div>
        </aside>
    </div>
    <% } else { %>
        <header class="article-header">
        <h1 class="journal-title article-not-found-header">
        Article not found.</h1>
        <p>
            Invalid URL or the article may have been removed.
            Please check the URL or contact the journal administrator for assistance.
        </p>
  <a href="/" class="nagivate-btn">Go Back Home</a>

        </header>
    <% } %>

</div>